# 和菓子店 予約承り表タブレットアプリ

仕様の正本＝リポジトリ外の仕様書（uketamawari_spec.md v1）。ここは実装で確定した決定事項の引き継ぎ。

## 概要
- 店頭タブレット（**iPad・Safari**。仕様書のAndroid/Mopriaから変更）で予約入力→A4承り表を印刷
- サーバなし・素のHTML/JS/CSS・IndexedDB保存。ビルド工程なし、index.htmlを開くだけ
- 公開: **https://maruendo.github.io/uketamawari/** （GitHub Pages・mainにpushで自動反映）
  - 2026-08-30にOrganization「maruendo」へリポジトリを移動。
    個人アカウント名をURLに出したくないという要望のため。旧URL(fumisato407-crypto...)は無効
- push手段: ポータブルgh（%LOCALAPPDATA%\gh-portable\bin\gh.exe）で認証済み。通常のgit pushが通る

## 確定した運用・設計判断
- **操作分担**: 商品選び＝店主、お客様情報画面だけタブレットをお客に渡す。
  その画面の間は上部メニュー（予約一覧・設定）を非表示＝body.customer-mode（他客の個人情報保護）
- **印刷レイアウト**: A4横1枚に承り表を左右2面（各A5相当・中央に切り取り線）。
  **左=お客様控・右=店控で中身が違う**（下の「承り表の版」参照）。明細は7行。
  店の指示による配置なので勝手に変えない
- 価格null商品＝単価テンキー手入力。商品×入数=1SKU。注文時点の名前/価格をコピー保存
- **菓子・包材の大枠と明細の商品内容はプルダウン入力**（種類＋個数を選ぶ。実装済み）。
  紙の承り表への店主の手書き指示（reference/photos/IMG_3624.JPG）
  - プルダウンで**選ぶ**候補は包材（箱・紙袋・風呂敷・クリアケース等）だけ。
    候補＝価格表の「箱」「紙袋」カテゴリ（2026-08-30）
  - **【重要】店控の右下の大枠＝その予約ぶんの「製造指示書」**（店主 2026-09-01）。
    売った商品名を並べる欄ではない。**何の菓子を何個作り、何の包材を用意するか**を書く。
    一度「商品名を並べる」と誤解して実装したので繰り返さないこと。組み立ては `shopBoxText()`:
    - 菓子: 中身(picks)を選んだ明細は、**中身の個数×その商品(箱)の個数**を数える
      （中身は1箱ぶんの入力なので、箱が複数ならその分だけ増やす。店主 2026-09-02）。
      選んでいない明細は**商品そのもの**を数える（大福×10 のような単品も作る菓子なので出す）
    - **【2026-09-02に変更】以前は「詰合せの箱数は掛けない」だったが逆になった**。
      紙の承り表に店主が入れた赤字修正（最中×11→37等、実測で検算し裏取り済み）で、
      実際に必要な総数と食い違うことが分かったため。「掛けない」に戻さないこと
    - 包材: 選んだ包材（個数はそのまま。箱の数と結び付いていないため掛けない）
    - **同じ名前は必ず合算**する（作る数の指示書なので、ばらばらだと数えられない）。
      明細の「商品内容」欄は入力どおりのまま＝合算しない。合算するのはこの枠だけ
    - 菓子と包材の間は1行あける
    - Chromeでは26行入れてもA4横1枚に収まる（2026-09-02の縮小後も同じ）。ただし店のプリンタは
      Chromeより収まりが悪い（下の「印刷サイズ」参照）ので、20行を超える予約は2枚に分かれうる
  - 明細の「商品内容」＝詰合せの中身。候補は菓子（products.js の sweet:true）
  - 選択結果は picks 配列が正で、紙に出す文字列(note/packaging)はそこから組み立てる
  - **紙に出す形は2種類ある（2026-09-02・店主指示で使い分けに変更）**:
    - 右下の菓子・包材の大枠＝「・最中×9」を**1行ずつ縦に**並べる（2026-09-01決定・維持）。
      横に「・」で繋げると読みにくいとのことで縦にした経緯があるので、大枠はこのまま戻さない
    - 明細表の「商品内容」欄＝**横に「・」でつなげて詰める**（2026-09-02に変更）。
      大枠と違って行の高さが決まっているため、1品ずつ改行すると行が間延びする、との指摘
    - `fmtPicks`（縦・大枠用）と`fmtPicksInline`（横・明細用）で分けてあり、
      内部の1件ぶんの表記（`pickLine`）だけ共有している。表示側の white-space は
      明細の商品内容=`td.c-note`がpre-line、菓子・包材の大枠=`.memo-box`がpre-wrap
  - **詰合せ1・2は中身の自動選択に対応（2026-09-02・店主指示）**。
    「商品が決まっているのに毎回手で選ぶのは二度手間」とのことで、明細に追加した瞬間
    `autoPicksFromContents()` が商品マスタの `contents`（価格表の「中身」列の生文字列。
    reference/make_products.py 参照）を読み取り、picks へ自動で入れる。
    - 表記ゆれ（笑窪→中津の笑くぼ、モナロン→もなろん 等）は `CONTENTS_ALIASES` で吸収。
      **「干菓子」＝京干菓子2個入が1袋（1個＝1袋）**、「水饅頭」＝水まんじゅう（いずれも店主確認 2026-09-02）。
      別名表は「今表示中の菓子の名前」と突き合わせるので、店主が該当の菓子を
      かくす／改名すると、それを含む詰合せの自動選択は黙って効かなくなる（エラーは出ない）。
      全角数字も算用数字に直してから読む
    - **確実に読み取れた分だけ自動で入れる**。1文字でも読み違えたら**何も入れず**、
      これまでどおり店主が手で選ぶ（中身を誤読して配るくらいなら空欄で気づいてもらう方が安全）。
      **2026-09-02 v28時点で全13件が読める**。経緯: 5は「どら焼き」が商品名になったv26で、
      9は「ガトー抹茶」＝ガトー小の別名（店主確認済み・価格でも裏取り 2400+3×160+3×190+350=3800）
      を足したv27で、7は価格表の「笑窪205」を店主確認のうえ「笑窪5」に直して（v28。
      reference/ の xlsx 自体を書き換えた。店の原本は未修正なので次に表をもらったら要確認）
      読めるようになった
    - **contents（価格表の中身欄）は起動時に既存の商品にも配る**（master.syncBundled・v28）。
      アプリで編集できない値なので店の修正と衝突しない。値段・名前は従来どおり配らない。
      これにより中身欄の修正は「価格表の内容に戻す」なしで届く（ヘッドレスで確認:
      店で値段を変えた商品でも contents だけ更新され値段は残る）
      **v26からは店主が商品編集画面の「詰合せの中身」で直接決められる**（fixedPicks・
      下の進捗v26参照）ので、価格表を直さなくてもよい。fixedPicks があれば contents は読まない
    - 自動で入った後も、他の選択と同じタグ操作（＋で足す／タップで消す）で直せる。
      客の希望で中身が定番と変わることは実際にある（紙の承り表でも価格は同じで
      中身が違う実例を確認済み）ので、あくまで初期値
    - オーダー詰合せ（contents無し・price null）はそもそも対象外＝常に手動のまま
  - 中身4件＋包材2件でもA4横1枚に収まることを確認済み（?demo=1 がこの重い例になっている。
    2026-09-02の縮小後は紙面の高さ113mm／194mm）
### 承り表の版（2026-08-30実装済み・reference/photos/承り表新レイアウト_2026-08-30.png）
  この縦レイアウトを1面分として**A4横2面・切り取り線は維持で確定**（2026-08-30）。
  上部が左右2列・明細の空行は細罫線・下部左=合計/支払い/備考、下部右=大枠（見出し：トータル数 菓子・包材）
  - **上部2列の中身（2026-09-02・店主指示で並び変更）**: 左=御名前/御住所/電話番号/支払、
    右=御来店日時/発送日/着日/熨斗・サイズ/表書き。表書きと支払いの位置を入れ替えた
    （表書き:左→右、支払い:右→左）。この並びはお客様控・店控で共通のコードなので両方に反映される
  - **御来店日時・発送日・着日は、折り返すと読みにくいので1行ずつ独立した行にした**
    （2026-09-02）。以前は「配送」の1行に発送日・着日をラベルごと詰め込んでいて、
    列の幅が足りず日付の曜日部分が折り返していた。3つとも御来店日時と同じ
    「ラベル+値だけの1行」にすることで解消（`.val.date-val`にnowrap。ヘッドレスで
    scrollWidth計測して折り返さないことを確認済み）
  - **印刷サイズ（2026-09-02・v23）**: 店のプリンタで横印刷が2枚に分かれた。店主の試しでは
    「82%に縮めるとA4に収まる」。**Chromeの計測では1枚（高さ160mm／194mm）で再現できず、
    端末側の原因は未特定**（iPad Safariは印刷時にその時の画面幅で組んでから紙幅に合わせて
    拡大縮小するので、縦持ちだと引き伸ばされて縦にはみ出す、が有力な仮説）。対策は2本立て:
    - 紙面の高さを2割強詰めた: `.sheet` の zoom 0.66→**0.60**、大枠の min-height 100mm→**60mm**
      （行が多ければ従来どおり伸びる）、明細のセル高 8/5mm→7/4mm、行の余白を0.5mmずつ縮小。
      結果（ヘッドレス実測）: 大枠9行=113mm（旧比70%）、12行=121mm（76%）、20行=151mm（94%）。
      「82%で収まる」に照らすと**12行程度までは確実、20行超は店のプリンタで2枚になりうる**
    - 印刷時の `#print-sheet` の幅を **281mm固定**（print.css。以前は auto）。
      上の仮説が正しければ端末の向きに関係なく同じ縮尺で出る
    - 文字は約1割小さくなった（値12pt×0.60=7.2pt相当）。読みにくいと言われたら、大枠の
      min-height をさらに下げる方を先に検討し、zoom はこれ以上下げない
- **2面は同一内容ではなくなる（2026-08-30確定）**: 左=お客様用・右=店用。
  **お客様用には菓子・包材の大枠を印刷しない**（店用のみ）。写真=reference/photos/承り表2面指示_2026-08-30.png
- **お客様用の面はレシート風の新デザイン**（reference/承り表_お客様用_2026-08-30.docx）:
  上部2列（左=名前/住所/電話/表書き、右=来店日時/配送/熨斗/支払）→明細→
  「ご予約ありがとうございました 合計」→**店名・住所・TELを印字**
  （菓子舗丸円堂 〒871-0021 大分県中津市沖代町1丁目446番11 TEL 0979-64-6328）。
  備考・トータル数・菓子/包材枠なし。**店控は午前のWordレイアウトのまま**（店主確認済み）
- **支払い表示（2026-08-30・最終）**: Wordの様式どおり**「済・未」を並べて該当側に丸**
  （熨斗の内・外と同じ見せ方）。一時は「済＝赤字でお礼文」の案が出たが**取り下げ済み**。
  蒸し返さないこと
- **支払いの運用**: お客はアプリ入力後レジで会計→店主が承り表の入力を仕上げて渡す。
  つまり支払い済/未の入力は**店主側の画面（印刷前）**で行う（お客様画面には出さない）
- **商品選択のカテゴリ＝レジの5タブに合わせる（2026-08-30確定）**:
  詰合せ1 / 詰合せ2 / 定番 / 生菓子 / 箱 店内
- **予約一覧の状態表示（2026-09-01・店主指示）**: 「受付済／受渡済」は一字違いで紛らわしく、
  さらに受渡済と支払済が混同されるという指摘。画面では
  **お渡し＝まだ／済・お支払い＝まだ／済** と見出し付きで並べ、今どちら側かを常に表示する
  （保存データの status は "受付済"/"受渡済" のまま。表示だけ言い換えている）。
  **押した瞬間には切り替えず必ず確認を出す**（ワンタッチだとどちらだったか分からなくなるため）。
  一覧で支払いを変えたときは、同じ予約を開いていれば state 側も揃える
  （揃えないと印刷画面の「保存」で一覧の変更が巻き戻る）
- **印刷プレビューの下ボタンは来た経路で変える（2026-09-01・店主指示）**:
  予約一覧から開いた＝[予約一覧に戻る][編集する][保存][印刷] /
  入力の流れ＝[編集に戻る][保存][印刷]。判定は state.openedFrom
- **予約IDの採番は同じ日の最大番号＋1**（2026-09-02）。以前の「件数＋1」は、予約を削除したあとの
  新規保存が既存IDと重なり、その予約を**黙って上書きして消していた**（A,B保存→A削除→C保存でBが消える。
  ヘッドレスで再現）。件数方式に戻さないこと
- **【2026-09-02・v33】入力中は上部メニューの「予約一覧」「設定」を押せない（入力中ロック）**。
  店主と相談して決めた（案1）。経緯: 商品を選ぶ途中で一覧へ飛び別の予約を「開く」と途中の入力が
  黙って消えていた。「見るだけモード」や確認の窓を足す案もあったが、店主が
  「ミスが絶対起きなさそう」「確認が毎回入るのもうざい」と案1を選んだ。
  - 入力中＝商品が1点以上／御名前か電話番号あり／一覧から開いた予約が乗っている（`isBusy()`）
  - ロック中は `.nav-btn.locked`（灰色）。押すと `#nav-notice` に案内が4秒出るだけで確認は出さない。
    「受注入力」には `● ` の印（`.busy`）
  - **入力中を終える出口は3つだけ**: プレビューの「保存」「印刷」（どちらも入力を空にして**予約一覧へ**）、
    受注入力の「クリア」（従来どおり確認あり）。一覧から開いた予約はプレビューの「◀ 予約一覧へ戻る」
    でも閉じられる（`isDirty()`＝読み込み／保存時点の `savedSnapshot` と今の内容が違うときだけ
    「変更を保存していません」の確認）
  - **印刷を押した時点でその注文は終わり**（店主）。`afterprint`（保険で `matchMedia("print")` の
    change も）で**印刷済み表示**に切り替える（v34・案C・2026-09-03）: 承り表はそのまま残し、
    下を「予約一覧へ」「次の予約を入力する ▶」の2つにする（`setPrintedMode`・`#printed-bar`）。
    どちらも resetOrder。**v33で「即・予約一覧へ」にしたら「いま何を印刷したか分からなくなる」と
    店主に言われて変えた**。renderSheet() で印刷済み表示は解除される。
    **iPad Safariで afterprint は飛ぶ（実機確認 2026-09-03）**。ただし**印刷の窓を開いた時点で**飛び、
    その後に紙面を組み直すので、印刷済み表示の帯とボタンが紙に印刷された（写真あり）。
    print.css で `#screen-preview > :not(#preview-scroll)` を消して解決（v35）。
    **プレビュー画面に要素を足すときは print.css の個別指定に頼らず、この包括ルールに任せる**
  - これに伴い v21 で入れた「受注入力を押したときの『◯◯様の予約が残っています』確認」
    （askResetSavedOrder）は**削除**した。入力中は他画面へ出られないので不要。
    「明細（保存済み：◯◯様）」の見出しは残してある
- **「◀ …へ戻る」は一段だけ前の画面へ。文字は行き先つき**（2026-09-02・v32→v33）。
  「戻る」だけだと経路によって行き先が違い迷う、との店主指摘。`state.prevScreen` を goto() で記録。
  お客様情報画面: 商品選択から来たら「◀ 商品選択へ戻る」、プレビューの「お客様情報を直す」から
  来たら「◀ 印刷プレビューへ戻る」（renderSheet してから戻る）。
  プレビュー: 一覧から開いた＝**[◀ 予約一覧へ戻る][商品を直す][お客様情報を直す][保存][印刷]**
  （v34・案B・2026-09-03。v33の「編集する」1つだと商品を足す道が見つからないと店主指摘。
  「商品を直す」→受注入力、「お客様情報を直す」→お客様情報）、入力の流れ＝[◀ お客様情報へ戻る][保存][印刷]
- **「保存して印刷プレビューへ」は文言どおり実際に保存する**（2026-09-02）。以前は保存せず、
  印刷か「保存」を押す前に他画面へ移るとお客様の入力が消えていた。保存に失敗してもプレビューへは進む
- **商品の編集はモーダルではなく独立した画面**（2026-09-01）。
  きっかけはキーボードの不具合調査だったが**それは誤診だった（下記）**。
  ただしタブレットでは全画面の方が押しやすく、実機で動作確認済みなのでこのまま。
  戻す理由がないので触らないこと

### 【重要】キーボードが出なかった件は **iPadOSが古かったのが原因**（2026-09-01・決着）
症状: 入力欄をタップするとフォーカスは当たる（カーソルが出る）のに、
ソフトキーボードだけが上がらない。アプリの全画面で発生。
**iPadOSをアップデートしたら直った。アプリのコードは無関係だった。**
- **同じ症状を見たら、まずiPadOSの更新有無を聞くこと。** コードを疑う前に。
  ここで私は v12→v14 まで3回、当て推量の修正を重ねて店主に無駄な検証をさせた
- 切り分けの型: **カーソルが出る＝タップもフォーカスも通っている**。
  CSSの覆いやタップ判定の問題ではない。そこでキーボードだけ出ないなら
  端末側（OSの不具合・物理キーボード接続）を先に疑う
- 実機で確定させる検査ページを作ってある。必要なら復活させる:
  `git show 5f9b1ef:kbtest.html > kbtest.html`
  （素の入力欄 / user-select:none付き / position:fixed内 / 本物のアプリ の4通りを試し、
   visualViewportの縮み方でキーボードが出たかを自動判定する。
   **検査1（素の入力欄）で出なければアプリは無関係**と即断できる）。
  原因が確定したので公開サイトからは消してある

### 【2026-09-04】iPadから印刷できない（「印刷」を押しても窓が出ない）＝ **iPadの再起動で直った**
Safariの他のページでも印刷できず、PCからは印刷できた。アプリは「印刷」で保存まで済ませて
window.print() を呼ぶだけなので、窓が出ないのはiPad側（AirPrintのUIが固まる）。
- 同じ症状なら**まず「Safariの別ページで印刷できるか」を聞き、できなければiPad再起動**。コードを触らない
- 切り分けの型: 「お支払い」を「いただいた」にして印刷→一覧で「済」になっていれば、
  アプリは印刷の直前まで動いている（印刷は保存してから窓を呼ぶ。支払いの状態は印刷の可否と無関係。
  ヘッドレスで両方確認済み）

### iPadのソフトキーボード周りで実際に直した本物のバグ
- **Enterでキーボードを閉じる処理は、日本語変換の確定Enterを必ず除外する**
  （`e.isComposing || e.keyCode === 229` で return）。除外しないと漢字に変換した瞬間に
  キーボードが消えて名前が打てない。**これは上のOS問題とは別の、実在したバグ**
- 入力欄の外をタップして閉じる処理は**即blurしない**。iOSは touchend → mousedown → focus の順なので、
  即blurすると「これから開くキーボード」を潰しうる。300ms待ってフォーカスが動いていないときだけ閉じる
- `user-select:none` は body ではなく「押すだけの部品」に限って掛けている。
  入力欄に効くとまずいという一般論からの予防であって、**今回の原因ではなかった**
- 動作確認は実機のみ。ヘッドレスでは `element.focus()` で focusin すら飛ばないことがある

### 版の届け方（「直したのに直っていない」の切り分け）
- **設定画面に版番号を出してある（APP_VERSION）**。まず店主に版番号を読んでもらう。
  **`APP_VERSION`（js/app.js）と `CACHE`（sw.js）は必ず同時に上げる**
- 設定画面の「アプリを最新にする」＝SW登録解除＋uketamawari-*キャッシュ削除＋再読込。
  店主が自分で最新版を取り直せる逃げ道（予約データはIndexedDBなので消えない）
- ホーム画面アプリは**1回閉じ直しただけでは新版にならない**（起動時に新SWが入り、
  次の起動でようやく新しいファイルが読まれる）。「2回閉じ直す」か上のボタンを案内する

- 画面の高さは **body を flex 縦積みにして .screen が余りを取る**。
  `calc(100% - 56px)` の決め打ちに戻すと、バックアップ促しバナーが出た日だけ
  下の保存・印刷ボタンが画面外にはみ出して押せなくなる
- iPad注意: Safariは未使用7日でサイトデータを消すことがある→運用は「ホーム画面に追加」必須＋JSONバックアップ
- **個人情報の方針（店主確認済み 2026-08）**: 顧客データはiPad内(IndexedDB)のみ・外部送信なし。
  アプリ原本はGitHub Pagesに置いてよい（個人情報は乗らないため）。タスク7のPWA化で営業中はオフライン動作に
- **バックアップ運用**: 予約データJSONを店のWindows PCの共有フォルダへ（店内Wi-Fi直、ネット非経由）。
  完全自動送信はiPadの制約で不可。Safariの「ダウンロード保存先」を共有フォルダに固定できれば実質2タップ
  →ホーム画面追加アプリで動くかは実機テストで確認、ダメなら保存先を選ぶ数タップ方式に落とす。
  iCloud同期に個人情報が乗らないよう保存先設定に注意。予約入力するiPadは1台に限定（端末間共有なし）。
  - **iOSではホーム画面アプリとSafariでストレージが分かれている可能性が高い**。
    「保存先が選べないときはSafariで開いて書き出す」という逃げ道を店主に案内しかけたが、
    それをやると**予約0件のまま書き出して前のバックアップを空で上書きする**恐れがある。
    手引きからは削除し、代わりに「書き出す前に『保存されている予約：◯件』を確認する」を入れた。
    未検証なので、実機で確かめるまでこの案内を復活させないこと
  PC側の共有フォルダ作成手順書はこちらで用意する（未着手）

## 検証のやり方（Streamlit等は無関係。ヘッドレスChromeで目視相当まで可能）
- 構文: `node --check js/app.js`
- **【重要・2026-09-01】`chrome --screenshot --timeout=...` はもう使えない**。
  Chrome 152 で --timeout が実時間を待たなくなり、IndexedDBの読み込みが終わる前に
  撮影されるようになった（商品も画面遷移も入っていない空の画面が写る）。
  file:// でもHTTPでも同じ。**「アプリが壊れた」ように見えるが壊れていない**ので早合点しないこと。
  → 代わりに **`tools/shot.js`（自作の最小CDPクライアント）** を使う:
  ```
  python -m http.server 8765 --bind 127.0.0.1
  chrome --headless=new --disable-gpu --no-first-run --remote-debugging-port=9222 \
         --user-data-dir=<使い捨て> --window-size=1280,900 about:blank &
  node tools/shot.js "http://127.0.0.1:8765/index.html?nosw=1&demo=1" out.png 4000 "JS式" ...
  ```
  実時間で待ってから撮る／任意のJSを評価できる（`await` 可・戻り値がそのまま出る）ので、
  ボタンを押した結果やIndexedDBの中身まで確認できる。出力先を `.pdf` にすると印刷イメージが出る。
  alert/confirm は評価の最初に `window.alert=()=>{}; window.confirm=()=>true;` で潰せる
  （＝ヘッドレスでconfirm経路も試せるようになった）
- **`?nosw=1` を付けないとService Workerが古いCSS/JSを返す**。
  ローカルHTTPで検証中に「直したのに変わらない」ときはまずこれ。付け忘れて半日溶かした前科あり
- `?nosw=1` を付けても、**使い回したプロファイルは普通のHTTPキャッシュで古いファイルを返す**。
  data/products.js のような更新頻度の低いファイルを直したら、**プロファイルを作り直してから検証する**
  （`SAMPLE_MASTER.products.length` を評価して期待値と合うか先に確かめると早い）
- 画面幅: ヘッドレスは幅500px未満に縮まらない。スマホ幅の検証は500pxで行い実機で最終確認
- URLパラメータ: `?demo=1`=ダミーデータ投入 / `&screen=screen-xxx`=画面直行 /
  `&tap=ボタンID`=自動タップ / `&selftest=1`=デモ予約を保存→一覧表示 /
  `&nosw=1`=Service Workerを登録しない（検証用。古いキャッシュを掴まないため）
- ボタンを追加したら必ず ?tap= で実際にタップ経路を確認（data-goto付け忘れ事故の再発防止）
- **PWA/Service Workerは file:// では一切動かない**。検証はローカルHTTPで:
  `python -m http.server 8765 --bind 127.0.0.1` → `http://127.0.0.1:8765/index.html`
  （localhostは安全なオリジン扱いなのでSWが登録される）
- **ヘッドレスの落とし穴**（2026-08-29に半日溶かした。以下は全部コードのバグではない）:
  - `alert()`を通るとレンダラが異常終了する。スクリーンショットが古いまま/консоль出力も消える。
    → alert前にDBへ書く設計にしてあるので、結果は**IndexedDBを直接ダンプして確認**する
  - `--enable-logging=stderr`のconsole出力は取りこぼす。出ない＝実行されていない、ではない
  - `--dump-dom`は`--timeout`を待たない。IndexedDB系の確認には使えない（画面は「読み込み中…」のまま）
  - SWのCacheStorage書き込みは、**fetchハンドラ側のcache.putをやめたら通るようになった**
    （install と同じURLへの書き込みが競合して "Entry already exists" で全滅していた）。
    設定画面の「アプリの状態」に 14/14 と出れば事前キャッシュは成功している。
    **fetchハンドラでキャッシュに書き戻さないこと**（またハマる）
  - ただし**サーバを止めた状態での起動はヘッドレスでは依然ERR_FAILED**。
    ブラウザを毎回起動し直すためSWが立ち上がる前にナビゲーションが失敗する模様。
    → **オフライン起動の確認は実機（iPadを機内モード）でしかできない**
  - ダウンロード（`a.click()`）はヘッドレスでは保存されずChromeが固まることがある
  - **使い回した--user-data-dirでは?tap=が不発になる**（同じURLでも新規プロファイルなら動く。
    demo=1等の同期処理は動くのにsetTimeout経由のtapだけ落ちる）。**tap検証は毎回新規プロファイルで**
  - `element.focus()` はヘッドレスだと focus/focusin が飛ばないことがある。
    focusinに紐づくUI（キーボードを閉じるボタン等）は `dispatchEvent(new FocusEvent("focusin",{bubbles:true}))`
    で確認する。**ソフトキーボードそのものの挙動はヘッドレスでは再現できない＝実機確認必須**

## 進捗（実装順1-7）
- 済: 1骨組み / 2受注入力 / 3顧客フォーム / 4印刷レイアウト＋2面化 / 5保存・予約一覧・再印刷
- 済: **7 PWA＋JSONバックアップ**（設定画面に「バックアップ」「アプリの状態」を追加）
  - `manifest.webmanifest` / `sw.js` / `icons/`（承の字・小豆色。Pillowで生成）/ `js/backup.js`
  - **sw.jsを直したら必ず CACHE の版番号を上げる**。上げないとiPadに修正が届かない
  - 書き出しは「記録をDBに保存→画面更新→最後にダウンロード発火」の順。
    ダウンロードを踏むと後続JSが中断されることがあるため、順序を変えないこと
  - 「バックアップから戻す」は confirm 3段（読み込むか→入れ替え/追加→全消しの最終確認）。
    分岐は askImportMode() に切り出してNodeで全パターン検証済みだが、
    **confirm/alertを通る経路はヘッドレスで動かせないので、実機で押して確認すること**
  - バックアップ促しバナー: 予約が1件以上あり、未実施 or 7日以上書き出していないと
    ヘッダー下に表示（backupReminderText()は純関数・Nodeでテスト済み）。お客様画面では非表示。
    「閉じる」は次の起動まで
  - 「バックアップから戻す」はボタンを押した瞬間に注意confirm（非常用である旨）→ファイル選択
  - 実機で要確認: ホーム画面追加→機内モードで起動 / 書き出し先にPC共有フォルダを選べるか /
    Safariの「ダウンロード保存先」を共有フォルダに固定できるか /
    読み込みの3段confirmが意図通り出て、1段目キャンセルで何も起きないこと /
    復元入口の注意confirmでキャンセル→ファイル選択が開かないこと・OK→開くこと（confirm直後の
    file input起動がiPadで動くか） / バナーの「閉じる」で消えて再起動で再表示されること
- 済: **商品データ109品（2026-09-02の価格表「商品値段2　2026.xlsx」2枚目より。オーダー詰合せ1〜5を含む）**（data/products.js。
  reference/make_products.py で生成。**手で編集しない**。価格表が変わったら
  xlsx2json_v2.py → make_products.py の順で作り直す。8月29日版110品からの差分は
  「店からの現物資料」の項を参照）
- 済: **承り表の新レイアウト**（お客様控＝docxのレシート風・店控＝包材枠つき／
  支払いは済・未に丸／商品内容・菓子包材のプルダウン／支払いトグルは印刷プレビュー画面）
- 済: **6 商品マスタ編集画面**（設定画面内）。**IndexedDBが正**になった:
  - 初回起動で data/products.js を取り込み、以後は店主の編集が残る（js/master.js）
  - 商品の追加/名前・値段・置き場所の変更/かくす/削除、担当者の編集、
    「価格表の内容に戻す」（同梱データで再取り込み。担当者は消さない）
  - 新規商品は id が `u_` 始まり。同梱データ（`p_`）と衝突しない
  - プルダウン候補は商品ごとの sweet / packaging フラグで決まる。
    店主が追加した商品は編集画面のチェックで指定する
  - master.load/save/reseed の挙動は12項目をブラウザ上のテストで確認済み
  - **同梱データに後から足した商品は、稼働中のiPadにも自動で届く**（master.syncBundled・2026-09-01）。
    起動のたびに同梱データと突き合わせ、**まだ届けていないidだけ**をputする。
    既にある商品には触らないので、店で直した値段・名前・かくす設定は消えない。
    届けたidは settings の `seededProductIds` に控え、**店主が消した商品を毎回復活させない**
    - この仕組み以前から使っている端末は、控えが無いので「今ある商品＝届け済み」とみなす。
      よって店主が過去に**削除**した同梱商品が一度だけ復活しうる（かくすなら影響なし）
    - **値段の変更は届かない**。既存idには触らない設計のため。
      値段を配り直すには従来どおり「価格表の内容に戻す」（店の修正は消える）
  - 価格表に無い商品を足すときは **reference/make_products.py の EXTRA** に書いて生成し直す。
    idは `p_x##`（価格表由来の`p_001`とも、店主が足す`u_`とも衝突しない）。
    例: オーダー詰合せ（詰合せ2タブ・price null＝注文のたびにテンキーで金額入力）
- 済: **予約一覧の状態表示（お渡し／お支払い×まだ・済＋確認ダイアログ）**、
  **印刷プレビューの下ボタンを経路で切替**、**版番号表示と「アプリを最新にする」**（2026-09-01）。
  いずれもヘッドレス（tools/shot.js）で確認済み・**実機での確認は未**
- 済: **実機印刷テスト（iPad→AirPrint→Canon TS3730）**（2026-09-01・店主確認）
- 残: 実機で2026-09-01分の確認 — 商品の追加でキーボードが出て保存できるか /
  一覧の「お渡し・お支払い」の確認ダイアログ / 予約一覧から開いたときの下ボタン4つ
- 残: 実機で2026-09-02分（v21）の確認 — 印刷後に上部メニュー「受注入力」で確認が出て空になるか /
  お客様画面の「保存して印刷プレビューへ」で予約一覧に載るか
- 済: **承り表レイアウトの微修正・製造指示書の掛け算・詰合せ中身の自動選択（v22）**（2026-09-02）。
  紙の承り表への店主の赤字修正指示より。表書き⇔支払いの入れ替え／御来店日時・発送日・着日を
  1行ずつに／明細の商品内容欄を横1行「・」つなぎに戻す（大枠は縦のまま）／
  右下大枠の菓子は中身×箱数で合算／詰合せ1・2は追加時に中身を自動選択（丸円堂詰合せ5・7・9の
  3件は価格表の記載が曖昧で対象外、上の該当箇所を参照）。ヘッドレスで確認済み・**実機での確認は未**
- 済: **印刷サイズの縮小（v23）**（2026-09-02）。店のプリンタで横印刷が2枚に分かれる件。
  詳細は上の「印刷サイズ」。ヘッドレスで9〜26行の高さとPDF枚数を確認済み・**実機での確認は未**
- 済: **干菓子の別名を京干菓子2個入に修正／承り表の住所が横にはみ出す件（v24）**（2026-09-02）。
  住所は `.sheet-row .val` に min-width:0 と overflow-wrap:anywhere。
  v21〜v23は 28b100b でpush済み（店のPCブラウザで v23 表示を確認）
- 済: **商品マスタを2026-09-02の価格表（タブ別シート）から作り直し（v25）**。110品→105品。
  reference/xlsx2json_v2.py を新設、make_products.py を「idを今の products.js から引き継ぐ」方式に変更。
  ヘッドレスで確認済み: 旧110品で種まきした端末に新版を入れると syncBundled で16品だけ増えて
  127品（改名前の名前が残る。別名表を候補順にしたので自動選択は旧名でも効く）→
  「価格表の内容に戻す」で105品・新名になる。**実機では未**
- 済: **オーダー詰合せを1〜5の5枠に分けた（v36）**（2026-09-07・店主指示）。同じ商品を2回押すと個数が
  増えるだけなので、中身の違うオーダー詰合せを2件作れなかった。make_products.py で「オーダー詰合せ」を
  5つに展開（p_x01=1番を引き継ぎ、p_x02〜p_x05）。**稼働中のiPadでは syncBundled で2〜5だけ増え、
  1番は「価格表の内容に戻す」を押すまで旧名「オーダー詰合せ」のまま**（名前は配らない設計のため）。
  105品→109品
- 済: **印刷済み表示の帯・ボタンが紙に印刷される件を修正（v35）**（2026-09-03・実機写真）。詳細は上の「入力中ロック」の印刷の項
- 済: **プレビューの編集ボタンを「商品を直す」「お客様情報を直す」に分割／印刷後は印刷済み表示（v34）**
  （2026-09-03・店主指摘）。詳細は上の「入力中ロック」「戻る」。ヘッドレスで回帰確認済み
  （入力の流れ→印刷→印刷済み→次の予約／一覧→商品を直す→商品追加→保存でDB反映／お客様情報を直す→
  戻る→確認なしで一覧／一覧→印刷→予約一覧へ／印刷済みのあと受注入力→プレビューで表示が消える／
  v29〜v31の確認類・一覧の削除も生きている）。**実機では未**。特に印刷後に印刷済み表示に切り替わるか
- 済: **入力中ロック／印刷・保存で予約一覧へ／戻るは一段前へ（v33）**（2026-09-02）。詳細は上の
  「入力中ロック」。ヘッドレスで8経路を確認済み（ロック・案内・戻るの往復・保存→一覧・開いて変更なしで
  戻ると確認なし・変更ありで確認→キャンセル留まる／OKで一覧かつDB不変・印刷→一覧）。
  **実機では未**。特に **iPadで印刷後に予約一覧へ移るか**を最初に見ること
- 済: **お客様画面の「◀ 戻る」を「◀ 商品選択へ」に改名（v32）**（2026-09-02・店主指摘）。予約一覧→開く→
  編集する→この画面、と来たとき「戻る」では印刷プレビューへ戻ると思われて混乱するため、行き先を書いた
- 済: **明細の個数を0にしてOKしたときも「削除しますか？」の確認を出す（v31）**（2026-09-02）
- 済: **選んだ中身・包材のタグは、押しても即消えず確認を出す（v30）**（2026-09-02・店主指摘
  「一回触っただけで消えるのはだめ」）。`renderPicks` 内で confirm。商品内容メモ・菓子包材・
  商品編集の「詰合せの中身」の3か所すべてに効く。ヘッドレスでキャンセル=不変／OK=消える を確認・**実機では未**
- 済: **商品編集「詰合せの中身」の戻すボタンを、手で変えた後にだけ出す＋確認つきに（v29）**（2026-09-02・店主指摘）。
  ヘッドレスで確認済み（空欄=ボタン無し／手で足す=出る／キャンセルで不変／OKで空に戻る）・**実機では未**
- 済: **詰合せ7「笑窪205」→「笑窪5」（店主確認）を価格表に反映／contentsを既存商品にも配る（v28）**
  （2026-09-02）。これで詰合せ13件すべて自動で中身が入る。ヘッドレスで確認済み・**実機では未**
- 済: **詰合せ9の「ガトー抹茶」＝ガトー小を別名表に追加（v27）**（2026-09-02）。
  店主から「7と9の中身が入っていない」と指摘。価格の合計が一致することで裏取りした。
  残るは詰合せ7のみ（上の「笑窪205」参照）
- 済: **詰合せの中身を店主がアプリで決められるようにした／どら焼きの改名／御来店日時のはみ出し（v26）**
  （2026-09-02）。いずれもヘッドレスで確認済み・**実機での確認は未**
  - **商品編集画面に「詰合せの中身」欄**。商品ごとに `fixedPicks`（[{id,name,qty}]）として
    IndexedDBに保存。明細に追加したときは `autoPicksFor()` が **fixedPicks を最優先**し、
    無ければ従来の contents 読み取り。名前は今のマスタから引き直す（改名に追従。かくした／
    消した菓子は設定時の名前で出す）。空欄のときは「価格表の記載どおり自動で入ります: …」と
    ヒントだけ出す（ボタンは無し。**v26では空欄時に「取り込む」ボタンを出していたが、
    店主の指摘（2026-09-02）で逆にした**: 空欄なら価格表の内容が自動で入るので取り込む必要が
    無く、要るのは手で変えた後に戻したいとき）。手で選んだ状態で価格表が読める商品にだけ
    「価格表の記載に戻す」ボタンが出て、**今の設定が消える旨の confirm を挟む**（v29）。
    読めない詰合せは「自動で読み取れません。ここで中身を選んでください」と出る
  - **「価格表の内容に戻す」でも fixedPicks は同じidの商品に引き継ぐ**（master.reseed）。
    価格表に無い情報なので消さない。confirm文にもその旨を入れた
  - **「丸円どら焼き」は店では「どら焼き」（200円）と同じ商品**（店主確認 2026-09-02）。
    make_products.py の `DISPLAY_RENAMES` で表示名を「どら焼き」にした（id p_114 は据え置き）。
    これで詰合せ5の「どら焼き4」も読めるようになり、曖昧な詰合せは7・9の2件になった。
    v25を入れた端末には「丸円どら焼き」の名で届いているので別名表に両方入れてある
  - make_products.py の **id引き継ぎのバグ修正**: 改名済みの products.js から作り直すと
    旧名でしか引かず、改名した8品に新しいid（p_127〜）を振ってしまっていた。
    「今の名前→旧名」の順で引くよう修正。**生成後は必ず `git diff data/products.js` で
    意図しないidの付け替えが無いか見ること**（付け替わるとiPadで商品が二重になる）
  - 御来店日時: PCでは1行に収まるがiPadの画面では「日付 時刻」が右にはみ出した（実機写真。
    ヘッドレスChromeは1024px幅でも12ptで収まり再現しない＝iPadのフォント幅の差と思われる）。
    一度「時刻だけ2行目に下ろす」案を実装したが**店主は「必ず1行」**とのことで取り下げ。
    代わりに `fitDateVals()`（js/app.js）が、入り切らない欄だけ文字を0.5ptずつ小さくして
    収める（下限7pt）。プレビュー表示時・resize・beforeprint で掛け直す。
    700px幅で10ptに縮んで1行に収まることを確認。**実機では未**
- 残: iPadで v25 を入れたあと、店主に断ってから「価格表の内容に戻す」を1回押す
  （押さないと消えた商品・改名前の名前が残ったまま）
- 残: 実機で2026-09-02分（v22/v23）の確認 — 承り表の見た目（入れ替え・1行化・詰め方）が意図通りか／
  詰合せ1・2をタップして商品内容が自動で入るか、直せるか／曖昧な3件が空のままか／
  **横印刷100%で1枚に収まるか（縦持ち・横持ちの両方で試す）**／文字の大きさが読めるか
- 残: 共有フォルダ運用の実機確認（下記の手引きに沿って店主が設定してみる）

## 店からの現物資料（reference/・**gitignore対象**）
店名＝**菓子舗 丸円堂**（まるえんどう。当初「丸の堂」と誤読していた）。2026-08-29受領。
GitHub Pagesは公開なので当面コミットしない。
- **`丸円堂価格表_2026-09-02.xlsx`（元の名前「商品値段2　2026.xlsx」）＝価格の正本（2026-09-02受領）**。
  2枚目のシート「2026.8 (2)」が正で、レジ／アプリの5タブごとに組み替えた表。実装は
  `価格表_2026-09.json` を読む。1枚目のシートは8月29日版と同じ内容
  - 8月29日版（`丸円堂価格表_最新.xlsx`・110品・`価格表_最新.json`）からの差分:
    **消す**（店主の備考）: 最中汁粉・霧箱詰合せ（桐箱と重複）・丸ぼうろ 6枚籠・懐中汁粉／
    **黒網カゴ→桐箱¥750** に差し替え／**改名**: 羊羹栗→栗羊羹カット、羊羹栗 1棹→栗羊羹棹、
    羊羹抹茶→抹茶羊羹カット、羊羹抹茶 1棹→抹茶羊羹棹、プリンほんわか→本和香プリン、
    水万十ゆず→柚子水まんじゅう、和菓子→生菓子、上用カートン→カートン箱代／
    **追加**: 生どら焼き280・丸円どら焼き200・キイチゴ大福250・やぶれ饅頭180・水ようかん180・
    わらび餅230・さざれ石琥珀等270・カップ水まんじゅう290（定番と生菓子の両方）、
    生菓子タブにも最中・笑くぼ・丸ぼうろ・もなろんの単品、箱タブに「箱」（価格未定）と「紙袋」20円／
    **箱タブから消えた**: 最中５/１０/１５の箱、丸ぼーろ10、ワッパ、赤飯桐箱3〜5号、紅白饅頭の箱4種、
    H25CB…の紙袋5種（「紙袋」1品に統合）、角底の袋3種、黒編み籠。結果 110品→105品
  - 表の備考「詰合せ１のところの写真が違うので写真なしで」はレジ側の話（アプリに写真はない）
  - 生菓子タブの羊羹は表のまま旧名（羊羹栗・羊羹抹茶）。定番タブだけ新名。表の表記どおり
- 単価手入力(price:null)は5件（オーダー詰合せ・丸円堂干菓子・生菓子・箱・送料）。
  価格欄が空欄／「━」＝未定（変換スクリプトで対応済み）
- `photos/` 26枚＝紙の承り表の記入見本＋手書き指示、レジ（TERAOKA）全カテゴリ画面、
  詰合せ商品の物撮り11枚（アプリの商品写真に使える）
- **レジ写真は旧価格が混じる**。価格が食い違ったらExcelを採る
- 8分類→5タブへの寄せ（旧 make_products.py の TAB/TSUME2）は2026-09-02に不要になった。
  価格表そのものがタブ別になったため、make_products.py はタブ名を読み替えるだけ
- その他の要確認事項（品名の揺れ・レジのみ/価格表のみの商品）は reference/README.md に一覧
- **新しい価格表が届いたら（2026-09-02以降の手順）**:
  1. `python reference/xlsx2json_v2.py "<新xlsx>" reference/価格表_YYYY-MM.json [シート名]`
     （タブ別シートを読む。旧形式の8分類一覧なら従来の xlsx2json.py）
  2. make_products.py の SRC をそのJSONにして `python reference/make_products.py`
  3. 備考欄に「←」で始まる店主の指示があれば、DELETE / REPLACE に書き足す。
     **書いていない指示があるとスクリプトは止まる**（黙って無視しない）
  4. idは今の data/products.js から（表示名, タブ）で引き継ぐ。改名は RENAMES に旧名を書く。
     引き継げないものだけ p_111〜 の新番号
  5. **稼働中のiPadに反映するには、設定画面の「価格表の内容に戻す」を1回押す**。
     syncBundled は新しいidを足すだけで、消えた商品・改名前の名前・価格変更は届かないため。
     店で直した値段・足した商品は消える（予約・担当者は残る）。押す前に店主に断ること
  **コンソールの文字化けは端末の表示だけでJSONは正常**
